# klang-linux-x64

Prebuilt `klang` binary for **linux-x64** (musl static build, works on Alpine/Termux-style environments).

This package is not meant to be installed directly – install `klang` instead,
which pulls this in as an optional dependency. The binary will be placed at
`bin/klang` by `npm-package/build.sh`.
